<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".MySplashScreen">

        </activity>
        <activity android:name=".PantallaDerrota" />
        <activity android:name=".PantallaVictoria" />
        <activity android:name=".MenuPausa" />
        <activity android:name=".MainJuego" ></activity>
        <activity android:name=".TutoActivity"/>
        <activity android:name=".OpcionesAct" />
        <activity android:name=".MainActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".ServicioMusica" android:enabled="true"></service>
    </application>

</manifest>
